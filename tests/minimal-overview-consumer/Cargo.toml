[package]
name = "minimal-overview-consumer"
version = "0.0.0"
edition = "2024"
rust-version = "1.97"
publish = false

[dependencies]
styrene-identity = { path = "../..", default-features = false }

[dev-dependencies]
async-trait = "0.1"
tokio = { version = "1", features = ["rt", "macros"] }

[workspace]
